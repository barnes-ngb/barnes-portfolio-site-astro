import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // Optional: set your canonical site URL once you have a domain
  // site: 'https://yourdomain.com',
});
